steps = ...previousSteps
steps -> next = working with profile page bio and image
-> next = show profile posts
-> next = new user registration feature


-> next = implement like and comment feature

-> next = implement create , update and delete post features


now in header chcck if it get profile then set its image otherwise set from auth 

-> next = complete update post (
    procedure=>
        (i) after clicking user will navigate to homepage
        (ii) post entry will focus to do this use lifting to the state up tecnique to visible
        (iii)
) 

-> authentication with firebase

